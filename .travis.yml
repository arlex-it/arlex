language: python
python:
    - "3.7.1"

install:
    - pip install --upgrade pip
    - pip install --upgrade -r requirements.txt

services:
  - mysql

script:
    - mysql -u root < bdd/script_sql/create_database.sql
    - |
      python main.py unit_test &
      APP_PID=$!
    #- python auto_test.py (unitest)
    - kill $APP_PID